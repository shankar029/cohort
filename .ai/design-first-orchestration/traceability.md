# Traceability — Design-first orchestration

| AC | Requirement | Design | Test | Evidence | Verdict |
|----|-------------|--------|------|----------|---------|
| AC1 | Design-first for non-trivial epics: Architect designs, then Lead splits/assigns | `designThenEnrich` front-loaded in `decomposeEpic`; step-5b architect removed | integration `designFirst.test.ts` "a non-trivial epic runs the Architect design turn and ENRICHES the tasks" | epic design persisted + task carries `<!--design-acceptance-->`; board floor non-empty | VERIFIED |
| AC2 | Complexity gate: trivial/simple skip design; Lead splits directly | `classifyComplexity` (pure) gates the design turn | unit `complexity.test.ts` (12) truth table; integration "a trivial change skips the design step" | trivial "fix a typo" → getEpicDesign empty, no enrichment, tasks still created | VERIFIED |
| AC3 | Fallback chain never strands the board (architect-absent → Lead; error/timeout → template) | template board is the synchronous floor; `designThenEnrich` is additive + time-boxed (`ATEAM_DESIGN_TIMEOUT_MS`) | integration "architect-absent Lead fallback" + "design turn times out → template + non-empty board" | Lead path enriches; at timeout=1ms tasks keep template desc, board non-empty | VERIFIED |
| AC4 | Builder personas decide under uncertainty (fixes greenfield FE stall) | QA-style clause added to frontend/backend/ux/data/devops personas (conditioned on empty/ambiguous) | unit `personas.test.ts` (14) prompt invariants | each builder has empty/greenfield escape hatch + irreversible-only escalation; brownfield reuse preserved; FE keeps its guard but no longer loops | VERIFIED (behavioral effect requires live LLM adapter — see report) |
| AC5 | Architect greenfield stack decision / brownfield honors docs | architect persona: greenfield decide+state stack; brownfield honor AGENTS.md/README/CONTRIBUTING; `designThenEnrich` stackClause steered by `brownfield` | unit `personas.test.ts` architect block; integration designFirst (brownfield fixture w/ AGENTS.md) | architect owns greenfield stack + honors AGENTS.md/CONTRIBUTING/README; brownfield design turn runs on ≥8-file fixture | VERIFIED (wording + orchestration; live design content requires live adapter) |
| AC6 | Agent-initiated bounded group discussion resolved by a decider (no user block) | `runGroupChat` bounded rounds + `pickDecider` routing + `<!--group-decision-->` write-back; `includesUser:false`; requester excluded (deadlock fix) | integration `groupDiscussion.test.ts` | builder opens group thread (includesUser=false, >1 participant); decider persists decision under group marker (not lead marker); no `question.updated` | VERIFIED |
