# Traceability — Design-first orchestration

One row per acceptance criterion. `design`, `test`, `evidence` are TBD (filled in Phase-2/3); `verdict`
is `pending` until implemented + verified. Evidence anchors point at the ground-truth seams from
`research.md`.

| AC | Requirement | Design (TBD) | Test (TBD) | Evidence (TBD) | Verdict |
|----|-------------|--------------|------------|----------------|---------|
| AC1 | **R1** Design-first for non-trivial epics: Lead creates a DESIGN task for the Architect FIRST; Architect produces the design; THEN Lead splits it into concrete per-stream tasks and assigns. | Insert pre-dispatch design turn in `decomposeEpic` (`orchestrator.ts:~899`) before `makeTask`; parse structured stream→task breakdown; persist via `setEpicDesign`; time-box with `awaitEpicDesign`-style bound so dispatch is still guaranteed. | Integration: non-trivial multi-stream chat → assert an Architect design turn runs BEFORE any `[stream]` task appears, and per-stream tasks reflect the design; assert `getEpicDesign(epic)` non-empty before builders start. | `orchestrator.ts` decomposeEpic `~899`, step-5b architect `~1148-1174`, `setEpicDesign`/`getEpicDesign` (`db/store.ts` epic-design section), `awaitEpicDesign` `~2612`, architect persona `catalog.ts:~40-45` | pending |
| AC2 | **R2** Complexity gate: trivial/simple changes SKIP the design step; the Lead splits directly. | Add pure `classifyComplexity(content, streams)` reusing `pickBrownfieldBuilders.multiLayer`, `scopeStreams` kept-count, `detectConstraints`, `isBrownfieldRepo`; branch to today's deterministic template when trivial. Must not block dispatch (scar-tissue invariant). | Integration: a trivial single-line request (e.g. "fix a typo in README") → assert NO Architect design turn runs and tasks are created directly by the template. | `streamScope.ts` `scopeStreams`/`pickBrownfieldBuilders`; `constraints.ts` `detectConstraints`; `orchestrator.ts` `isBrownfieldRepo` `~880`, `shortGoal` `~5115`; absence-proof: no `trivial/complexity` classifier exists | pending |
| AC3 | **R3** Fallback chain never strands the board: Architect absent → Lead does requirement+codebase analysis and splits; design error/timeout → deterministic template; board NEVER has zero assigned tasks. | Gate design on `specialists().some(s=>s.name==='architect')` (same predicate as `~2615`); on absent → time-boxed Lead analysis turn then split; on error/timeout (swallowed `try/catch` + `awaitEpicDesign` bound) → fall through to `makeTask` template as terminal fallback. | Integration ×3: (a) no architect on team → tasks still created (Lead path); (b) architect turn forced to error/timeout → deterministic tasks appear; (c) both paths → assert ≥1 assigned task exists within the bound (never-strand). | Scar-tissue NOTE `orchestrator.ts:~925-935`; `scopeStreams` zero-builder safety net (`streamScope.ts`); `scopedSpecialists` fallback `~1900-1915`; step-5b `try/catch` `~1148-1174` | pending |
| AC4 | **R4** Builder personas decide under uncertainty: frontend/backend/ux/data/devops detect conventions; if repo empty/ambiguous, pick mainstream default, state the assumption, proceed (build first; ask only for irreversible/product decisions). Fixes the greenfield FE "no code" stall. | Edit persona strings in `catalog.ts` to add QA-style wording; condition the "pick a default" license on empty/ambiguous so brownfield "reuse existing patterns" behavior is preserved. | Integration (greenfield): FE-only build on an EMPTY repo → assert real files produced (no empty-build "produced no code" question), i.e. FE picks a default stack and builds. Contrast with brownfield fixture (AC5). | FE stall sentence `catalog.ts:~68-73` ("matches the project’s existing framework … never introduce a new pattern when one already exists"); QA license `catalog.ts:~90-95` ("if none exists, pick a sensible one and add it"); empty-build gate `orchestrator.ts:~2740-2830` | pending |
| AC5 | **R1 (greenfield/brownfield)** Architect greenfield stack decision honoring docs: greenfield → decide tech stack/conventions/frameworks; brownfield → design within existing repo honoring any AGENTS.md/README/CONTRIBUTING/docs. | Extend Architect design prompt (greenfield: decide + state stack; brownfield: read AGENTS.md/README/docs then design). Reuse `isBrownfieldRepo` to steer. Tolerate absent AGENTS.md (none at repo root). | Integration ×2 with a NEW brownfield fixture helper (≥8 source files, optional `AGENTS.md`): (a) greenfield → design names a concrete stack; (b) brownfield → design references existing conventions/AGENTS.md and does not invent a new stack. | `isBrownfieldRepo` `~880-897`; architect persona `catalog.ts:~40-45`; absence-proof: no brownfield test fixture today; no root AGENTS.md | pending |
| AC6 | **R5** Agent-initiated bounded group discussion resolves via a decider: an agent opens a group thread, pulls in needed participants (PM/Lead/peer), discusses with BOUNDED rounds, resolves via a decider (Lead, or PM for product), writes the decision back — instead of blocking on the user. Reuse existing thread infra. | Extend `runGroupChat`/`maybeHandleGroupChatRequest`: add round cap (env-tunable), `decider` (lead|pm) routing, targeted participant selector, and decision write-back using the `<!--decision-->` marker + `updateWorkItem`/`emitEvent` pattern. Reuse `store.createThread`/`closeThread`. | Integration: a builder emits `[[REQUEST_GROUPCHAT: …]]` (or `[[NEEDS_DISCUSSION]]`) → assert a `group` thread is created with the right participants, rounds are bounded, a decider posts a decision that is persisted (marker on the work item), and NO human `question.updated` is raised. | `runGroupChat` `~1200-1265`, `maybeHandleGroupChatRequest`/`GROUPCHAT_RE` `~68`,`~1288-1305`, `requestGroupChat` app tool `~4685`, `pickDiscussants` `~1330`; reuse `LEAD_DECISION_MARKER` `~2035`, `leadResolveBlocker` `~2050-2130`, `raiseQuestion`/`noCodeQuestions` `~5030`, `mention.ts`; thread infra `db/store.ts` threads section | pending |
