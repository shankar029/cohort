Use **Vite + React + TypeScript** with **Vitest + Testing Library** because this is greenfield, small, and benefits from fast setup, typed UI code, and simple unit tests. Project layout: `src/counter.ts` for pure counter logic, `src/App.tsx` for the accessible page, `src/main.tsx`, `src/App.css`, and colocated `*.test.ts(x)` tests. Shared contract: `createCounter(initial?: number): { value: number; increment(): number; reset(): number }`, where `increment()` returns the next count and `reset()` returns `0`; the UI displays the current count and calls only this module for state transitions. Accessibility contract: the count is visible text, updates in an `aria-live="polite"` region, and both buttons are native `<button>` elements with clear labels.

[frontend] Build accessible click counter UI and counter module :: Vite React TypeScript app exists; page shows current count, Increment button, and Reset button; Increment increases count by 1 using `createCounter`; Reset returns count to 0; count updates in a polite live region; UI is keyboard-operable with native buttons.

[qa] Unit and component test coverage :: Vitest tests cover `createCounter` initial value, increment, and reset behavior; Testing Library tests verify rendered initial count, increment click updates count, reset click restores 0, and buttons are discoverable by accessible role/name.