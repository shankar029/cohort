# Traceability — Live Greenfield Smoke

| AC | Requirement | Design | Test | Evidence | Verdict |
|----|-------------|--------|------|----------|---------|
| AC4 | On a greenfield repo, a builder (Frontend) DECIDES under uncertainty and PRODUCES REAL CODE FILES instead of looping on clarifying questions. | `design-first` scenario + `scoreDesignFirst` (frontend attribution / sole-author fallback) | `evals/harness.selftest.mjs` (scoring); LIVE `design-first` (behavior) | run1/run2: FE stalled → user escalation; `frontendProducedCode=false` both runs; findings.md §root-cause | **NOT-VERIFIED (live)** — FE still stalls; product defect in design-first (PR #4), reopened as follow-up #1/#3 |
| AC5 | The Architect (or the Lead when no architect) STATES a concrete greenfield stack and a DESIGN IS PERSISTED before builders act. | `designThenEnrich` (observed) + `readEpicDesigns`/stack lexicon | `harness.selftest.mjs` (scoring); LIVE `design-first` (behavior) | run1/run2: `epic_designs` empty; architect turn returned 0-length text; `designPersisted=false` | **NOT-VERIFIED (live)** — empty architect turn silently no-ops design-first; reopened as follow-up #1/#2 |
| AC-H | The harness is REPEATABLE, OPT-IN (real adapter only when explicitly selected), BOUNDED (per-turn + scenario timeouts; isolated port/DB/worktree; no writes to the Cohort repo), and scores AC4/AC5 correctly. | `readEpicDesigns`, `deliverablesByStream`, `scoreDesignFirst`, `monitorUntil(snap,h)`, `design-first` scenario | `evals/harness.selftest.mjs` (7 tests) + `smoke --fake` regression | 7/7 self-tests pass; fake plumbing scores; 2 live runs bounded+isolated, `authIssue=false`; lint+typecheck clean | **VERIFIED** — harness works; correctly measured a true-negative with a root cause |
